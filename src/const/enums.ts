export enum LAYOUT {
  CARD = "card",
  LIST = "list",
}

export enum DELETE_RESPONSE {
  DELETED = "DELETED",
  NOT_FOUND = "NOT_FOUND",
}

export enum TOGGLE_RESPONSE {
  PINNED = "PINNED",
  UNPINNED = "UNPINNED",
}